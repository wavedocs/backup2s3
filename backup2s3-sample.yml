#
# AWS_S3 BUCKET ACCESS_KEY_ID and SECRET_ACCESS_KEY
#
aws_access_key_id: ACCESS_KEY_ID
aws_secret_access_key: SECRET_ACCESS_KEY
aws_bucket: BUCKET

tmp_path: /tmp

dirs:
  - /etc

mysql:
  host: localhost
  user: root
  password: password
  databases: []

